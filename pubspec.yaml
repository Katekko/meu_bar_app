name: ekko
description: A boilerplate for your project starts right.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  get: ^4.6.1
  get_storage: ^2.0.3
  logger: ^1.1.0
  equatable: ^2.0.3
  flutter_iconpicker: ^3.1.2
  rxdart: ^0.26.0
  money2: ^3.1.2
  currency_text_input_formatter: ^2.1.5
  uuid: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  test:

  flutter_lints: ^1.0.4
  build_runner: ^2.1.4
  json_serializable: ^5.0.2
  faker: ^2.0.0
  mocktail: ^0.2.0

flutter:
  uses-material-design: true